import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  imageContainer: {
    borderWidth: 1,
    borderColor: '#f35bac',
    padding: 3,
  },
  image: {
    borderRadius: 100,
  },
});

export default style;
